export const EnsureAuthentication = (props: any) => {
	console.log('props', props);
	//aqui você verifica autenticação e faz a logica
	/*if (!isAuthenticated()) {
        return <Redirect to={'logout'} />
    } else {
        return <RenderRoutes routes={routes} {...props} history={props.history} />
    }*/
	return {};
};
